[package]
name = "cameleon-device"
version = "0.1.0"
authors = ["Cameleon Project Developers"]
edition = "2018"

[dependencies]
thiserror = "1.0.20"
log = "0.4.11"
semver = "0.9.0"

# Dependencies for usb3 vision device driver
rusb = { version = "0.6.2", optional = true }

[features]
default = ["usb3"]

usb3 = ["rusb"]
